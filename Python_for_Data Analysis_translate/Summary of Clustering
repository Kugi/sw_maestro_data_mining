- K-Means 
  cluster 수를 지정(입력)
  
  매우 큰 samples수에 사용, MiniBatch code와 함께 중간 크기의 clusters에 사용
  (MiniBatch code는 실행시간을 줄여주고, 각 입력 데이터를 부수적인 서브셋들로 각 반복마다
  랜덤하게 정한다.)
  일반적으로 많이 사용되며, 정해진 cluster size에 사용된다. 2차원 적이고, 많지 않은 cluster를 갖는다.
  
- Affinity propagation
  damping, 샘플 우선순위를 입력
  
  크지 않은 samples수, 많은 cluster에 사용하고, 정해지지 않은 cluster size에 사용, 
  2차원 적이지 않은 평면(기하학)에서 사용
  
- Mean-shift
  bandwidth 입력.
  
  크지 않은 샘플수에 이용. 
  많은 cluster에 사용하고, 정해지지 않은 cluster size에 사용한다.
  2차원 적이지 않은 평면에서 사용
  
- Spectral clustering 
  cluster 수를 지정.
  
  중간 크기의 samples수, 적은 cluster에 사용.
  적은 cluster와 정해진 cluster size, 2차원 적이지 않은 기하학
  
- Hierarchical clustering
  cluster 수를 지정.
  
  큰 sample수와 cluster수 
 
- DBSCAN
  neighborhood size 입력.
  
  매우 큰 샘플수와 정해지지 않고 중간 크기의 cluster, 기하학적이지 않는 평면
  
- Gaussian mixtures
  많은 parameters 입력.
  
  크지 않는, 기하학적인 평면, 밀도 측정에 좋다.
